# Mac Timer

App semplice per spegnere, riavviare o mandare in sleep il Mac dopo un timer.
Scritta in Python (tkinter) e pacchettizzata con PyInstaller.

## Avvio dell'app (versione sorgente)

```bash
python app.py